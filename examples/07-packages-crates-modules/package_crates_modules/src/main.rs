fn main() {
    /*
     * paths:
     * allow to name items, use of `use` keyword that brings a path into scope
     * pub keyword: make items public
     * as keyword
     * external packages
     * glob operator
     */
}
